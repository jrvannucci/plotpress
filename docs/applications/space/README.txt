.. _app_space:

Astronomy and space science
---------------------------

Photometry, radio dynamic spectra, aperture-synthesis beams and rotation curves.
Astronomical quantities routinely span many decades, are quoted in magnitudes
that run backwards, and arrive with per-point uncertainties that are the whole
argument -- so log axes, inverted axes and error bars are the defaults rather
than the decorations.
