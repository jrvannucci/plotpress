.. _mesh_gallery:

Gridded fields
--------------

Scientific 2-D fields drawn with ``pcolormesh``: masked data, isolines over a
mesh, signed and heavy-tailed color scales, vector overlays, and curvilinear
grids. Every mesh becomes a single embedded ``<image>``, so a 300x300 field is
one element rather than 90,000 vector cells.
