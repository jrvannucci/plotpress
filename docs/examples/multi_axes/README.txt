.. _multi_axes_gallery:

Multi-axes layout and annotation
===================================

Techniques that sit on top of a plot type rather than being one: arranging
several axes on one figure, a second y-axis sharing the same x-axis, a
colorbar shared across axes, and annotating a point on the data.
